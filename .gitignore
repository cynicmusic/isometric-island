# dependencies / build output
node_modules/
dist/

# test + screenshot artifacts
artifacts/

# local dev-only state written by the Vite middleware (repo-root only —
# public/presets.json is the intentionally-tracked baked deploy snapshot)
/sticky.json
/presets.json

# os / editor
.DS_Store
*.log
